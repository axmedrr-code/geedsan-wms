# /etc/fail2ban/filter.d/nginx-limit-req.conf
#
# Detects IPs that nginx is rate-limiting (HTTP 429 responses).
# Matches the "cloudflare" log format configured in nginx.conf.

[Definition]
failregex = ^<HOST> .+ (GET|POST|PUT|DELETE|PATCH) .+ 429 .*$
ignoreregex =
