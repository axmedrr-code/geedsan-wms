# /etc/fail2ban/filter.d/nginx-auth-fail.conf
#
# Detects repeated HTTP 401 responses on the API auth endpoints.

[Definition]
failregex = ^<HOST> .+ POST .+/api/auth/(login|refresh).+ 401 .*$
ignoreregex =
